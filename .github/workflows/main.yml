name: Update Ncurses (Release)

on:
  schedule:
    - cron: "0 0 * * 0"
  workflow_dispatch:

jobs:
  release-ncurses:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install required tools
        run: |
          sudo apt-get update
          sudo apt-get install -y wget dpkg-dev rsync zip file

      - name: Set Ncurses version
        id: get_version
        run: |
          VERSION="6.5.20240831-3"
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Download, verify, normalize paths, and zip per-ABI
        run: |
          VERSION="${{ steps.get_version.outputs.version }}"
          BASE_URL="https://packages.termux.dev/apt/termux-main/pool/main/n/ncurses/"

          declare -A archs=(
            [aarch64]=aarch64
            [arm]=arm
            [i686]=i686
            [x86_64]=x86_64
          )

          rm -rf dist work
          mkdir -p dist work

          for arch in "${!archs[@]}"; do
            DEB_NAME="ncurses_${VERSION}_${arch}.deb"
            URL="$BASE_URL$DEB_NAME"

            echo "Downloading $DEB_NAME from $URL"
            wget -q --show-progress "$URL" -O "$DEB_NAME"

            # Verify it is a Debian archive
            FILE_TYPE=$(file "$DEB_NAME")
            if [[ "$FILE_TYPE" != *"Debian binary package"* ]]; then
              echo "Error: $DEB_NAME is not a valid Debian archive!"
              exit 1
            fi

            # Extract deb
            mkdir -p "work/$arch"
            dpkg-deb -x "$DEB_NAME" "work/$arch"

            # Normalize layout: copy usr/ only
            mkdir -p "dist/ncurses-$arch/usr"
            rsync -a "work/$arch/data/data/com.termux/files/usr/" \
                     "dist/ncurses-$arch/usr/"

            # Zip per architecture
            cd dist
            zip -qr "ncurses-${VERSION}-${arch}.zip" "ncurses-$arch"
            cd ..
          done

      - name: Publish GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ncurses-${{ steps.get_version.outputs.version }}
          name: Ncurses ${{ steps.get_version.outputs.version }}
          body: |
            Ncurses binaries built from Termux packages.

            Path normalization applied:
            - Removed /data/data/com.termux/files
            - Root starts at /usr

            Included architectures:
            - aarch64
            - arm
            - i686
            - x86_64
          files: dist/*.zip
